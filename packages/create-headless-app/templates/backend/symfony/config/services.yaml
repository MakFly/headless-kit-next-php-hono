# yaml-language-server: $schema=../vendor/symfony/dependency-injection/Loader/schema/services.schema.json

# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.
# See also https://symfony.com/doc/current/service_container/import.html

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones

    # BetterAuth - Configure repositories to use App entities
    BetterAuth\Symfony\Storage\Doctrine\DoctrineUserRepository:
        arguments:
            $userClass: 'App\Shared\Entity\User'

    BetterAuth\Symfony\Storage\Doctrine\DoctrineSessionRepository:
        arguments:
            $sessionClass: 'App\Shared\Entity\Session'

    BetterAuth\Symfony\Storage\Doctrine\DoctrineRefreshTokenRepository:
        arguments:
            $refreshTokenClass: 'App\Shared\Entity\RefreshToken'

    BetterAuth\Symfony\Storage\Doctrine\DoctrineMagicLinkRepository:
        arguments:
            $tokenClass: 'App\Shared\Entity\MagicLinkToken'

    BetterAuth\Symfony\Storage\Doctrine\DoctrineEmailVerificationRepository:
        arguments:
            $tokenClass: 'App\Shared\Entity\EmailVerificationToken'

    BetterAuth\Symfony\Storage\Doctrine\DoctrinePasswordResetRepository:
        arguments:
            $tokenClass: 'App\Shared\Entity\PasswordResetToken'

    BetterAuth\Symfony\Storage\Doctrine\DoctrineTotpRepository:
        arguments:
            $totpClass: 'App\Shared\Entity\TotpData'

    BetterAuth\Core\Interfaces\TokenSignerInterface:
        alias: BetterAuth\Symfony\Service\EventDispatchingTokenService

    # Override user class from App\Entity\User to App\Shared\Entity\User (post-VSA refactoring)
    BetterAuth\Symfony\Security\BetterAuthUserProvider:
        arguments:
            $entityManager: '@Doctrine\ORM\EntityManagerInterface'
            $idConverter: '@BetterAuth\Symfony\Service\UserIdConverter'
            $userClass: 'App\Shared\Entity\User'
        public: true

    BetterAuth\Symfony\Service\UserIdConverter:
        arguments:
            $entityManager: '@Doctrine\ORM\EntityManagerInterface'
            $userClass: 'App\Shared\Entity\User'
        public: true
